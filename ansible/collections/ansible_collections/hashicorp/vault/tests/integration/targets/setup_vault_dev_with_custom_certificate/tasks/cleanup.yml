---
- name: Stop container infrastructure
  ansible.builtin.command: >-
    {{ container_runtime }} compose down --remove-orphans
  args:
    chdir: "{{ vault_temp_dir }}"

- name: Delete temporary directory
  ansible.builtin.file:
    state: absent
    path: "{{ vault_temp_dir }}"
